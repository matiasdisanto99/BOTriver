services:
  - type: web
    name: BOTriver
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y chromium chromium-driver && pip install -r requirements.txt
    startCommand: python river_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
